# ─── Frontend (auto-managed by Lovable Cloud) ───────────────
# These are publishable keys, safe for client-side use.
# The actual .env file is auto-generated — do NOT edit it.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key
VITE_SUPABASE_PROJECT_ID=your-project-id

# ─── Backend-only (set via Lovable Cloud secrets) ────────────
# These are NEVER exposed to the frontend.
# Configure them in Lovable Cloud → Backend → Secrets.
#
# SUPABASE_URL=              (auto-provided)
# SUPABASE_SERVICE_ROLE_KEY= (auto-provided)
# SUPABASE_ANON_KEY=         (auto-provided)
# SHOPIFY_API_KEY=           (from Shopify Partner Dashboard)
# SHOPIFY_API_SECRET=        (from Shopify Partner Dashboard)
# APP_URL=https://sonicinvoice.lovable.app
